import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xFF00923F); // Primary Color (Purple)
  static const secondaryColor =
      Color(0xFF049444); // Secondary Color (Dark Purple)
  static const accentColor = Color(0xFF049444); // Accent Color (Bright Blue)
  static const lightPurple = Color(0xFFDA231C); // Light Purple
  static const successColor = Color(0xFF09FF52); // Success Color (Green)
  static const warningColor = Color(0xFFF6FF00); // Warning Color (Yellow)
  static const errorColor = Color(0xFFFF0000);
  static const dashboardBackground = Color(0xFF00923F);
  static const white = Colors.white;
  static const card1 = Color(0xFF95959C);
  static const card2 = Color(0xFF88888c);
}
